[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rcraft_cython"
version = "0.1.3"
description = "Shared Object Interface between C++/Python"
authors = [
  { name = "Edwin Reidlo" }
]
dependencies = [
]
requires-python = ">=3.7"
